# FastapiServiceOrderservice

Microservice generated from prompt: Create Python FastAPI service OrderService

## Overview

This microservice was generated by the Golden Path Generator.

**Generated:** 2026-02-10T16:05:50.893732  
**Author:** Golden Path Generator

## Features

- api

## Quick Start

### Prerequisites

- Python 3.11+
- pip

### Installation

```bash
# Create virtual environment
python -m venv venv
source venv/bin/activate  # Linux/Mac
# or
.\venv\Scripts\activate  # Windows

# Install dependencies
pip install -r requirements.txt
```

### Running Locally

```bash
# Start the server
uvicorn src.main:app --reload --port 8000
```

### Using Docker

```bash
# Build and run with Docker Compose
docker-compose up --build

# Or build manually
docker build -t fastapi-service-orderservice .
docker run -p 8000:8000 fastapi-service-orderservice
```

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/` | Root endpoint |
| GET | `/health` | Health check (liveness) |
| GET | `/ready` | Readiness check |
| GET | `/docs` | Swagger documentation |

## Project Structure

```
fastapi-service-orderservice/
├── src/
│   └── main.py          # FastAPI application
├── tests/
│   └── test_main.py     # Unit tests
├── Dockerfile           # Docker configuration
├── docker-compose.yml   # Docker Compose
├── azure-pipelines.yml  # CI/CD pipeline
├── requirements.txt     # Python dependencies
└── README.md           # This file
```

## Testing

```bash
# Run tests
pytest tests/ -v

# Run with coverage
pytest tests/ --cov=src --cov-report=html
```

## License

MIT License - 2026
