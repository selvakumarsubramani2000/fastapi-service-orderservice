# FastapiServiceOrderservice Docker Compose
# Generated by Golden Path Generator

version: '3.8'

services:
  fastapi-service-orderservice:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - DEBUG=true
    volumes:
      - .:/app
    depends_on:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3



volumes:
